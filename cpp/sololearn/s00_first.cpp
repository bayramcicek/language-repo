// C11 standard
// created by cicek on Jan 24, 2021 2:53 PM

/*
 * C++ is a popular cross-platform language that can be used to
 * create high-performance applications - operating systems,
 * browsers, video-games, art applications and so on.*/

// #include is used for adding a standard or user-defined header files to the program.
#include <iostream>
#include <string>
//The <iostream> header defines the standard stream objects that input and output data.

/*A namespace is a declarative region that provides a scope to the identifiers (names of elements) inside it.
In our code, the line using namespace std; tells the compiler to use the std (standard) namespace:*/
using namespace std;
// The std namespace includes features of the C++ Standard Library.

// clang-format -i ./sololearn/s00_first.cpp

int main(int argc, char **argv) {
    /*
     * cout is the stream object used to perform output on the standard
     * output device which is usually the display screen.
        cout is used in combination with the insertion operator <<.*/

    // In C++, streams are used to perform input and output operations.
    cout << "merhaba" << endl;
    cout << "c++ \n"; // The new line character \n can be used as an alternative to endl.
    cout << "c with classes";
    // The backslash (\) is called an escape character, and indicates a "special" character.

    /* Adding comments to your code is a good practice.
     It facilitates a clear understanding of the code for you and for others who read it. */


    return 0;
}
